# InvestDash SKILLS.md v1.0

> 이 문서는 InvestDash 시스템의 전체 동작 규칙을 정의합니다.
> 코드를 수정하지 않고 이 파일만 변경해도 시스템 동작을 제어할 수 있습니다.

---

## 목차

1. [데이터 스키마 감지 규칙](#1-데이터-스키마-감지-규칙)
2. [금융 지표 계산 규칙](#2-금융-지표-계산-규칙)
3. [시각화 선택 규칙](#3-시각화-선택-규칙)
4. [AI 인사이트 생성 규칙](#4-ai-인사이트-생성-규칙)
5. [대시보드 레이아웃 규칙](#5-대시보드-레이아웃-규칙)

---

## 1. 데이터 스키마 감지 규칙

> 대응 모듈: `src/engine/schemaDetector.ts`

### 목적
업로드된 CSV/JSON 데이터의 컬럼을 분석하여 데이터 타입을 자동 분류한다.

### 우선순위 감지 규칙 (높은 우선순위 먼저)

#### STOCK_OHLCV (주가 OHLCV 데이터)
조건: 다음 컬럼 패턴이 모두 존재
- 날짜 컬럼: date | time | timestamp | 날짜 | 일자 | 거래일
- 가격 컬럼 (필수 4개): open + high + low + close (또는 한국어: 시가+고가+저가+종가)
- 거래량 (선택): volume | vol | 거래량

신뢰도:
- open+high+low+close+volume 모두 있을 때: 0.97
- open+high+low+close만 있을 때: 0.88

출력:
```
dataType: "STOCK_OHLCV"
primaryKey: date 컬럼명
valueColumns: [open, high, low, close, volume]
```

#### PORTFOLIO (포트폴리오 보유 현황)
조건 (중 하나 이상):
- ticker|symbol|종목코드 + weight|allocation|비중 존재
- asset|종목 + value|amount|평가액 존재

신뢰도: 0.92

출력:
```
dataType: "PORTFOLIO"
assetColumn: ticker/symbol/asset 컬럼명
weightColumn: weight/allocation/비중 컬럼명
```

#### RETURNS (수익률 시계열)
조건:
- date|날짜 컬럼 + return|returns|수익률|pnl|profit 컬럼 존재
- OHLCV 4개 컬럼은 없어야 함 (STOCK_OHLCV보다 낮은 우선순위)

신뢰도: 0.90

출력:
```
dataType: "RETURNS"
dateColumn: 날짜 컬럼명
returnColumn: 수익률 컬럼명
```

#### FINANCIAL_STATEMENT (재무제표)
조건:
- revenue|sales|매출|매출액|net_income|영업이익|eps 중 하나 이상

신뢰도: 0.85

#### GENERIC (기본)
위 조건 중 해당 없음. 신뢰도: 0.50. 대시보드: 기본 통계 + 막대/선 차트

### 예외 처리 규칙
- 컬럼 수 < 2: ERROR "최소 2개 이상의 컬럼이 필요합니다"
- 숫자 데이터 비율 < 30%: WARNING "숫자 데이터 비율이 낮습니다"
- null 비율 > 50%: WARNING "[컬럼명] 컬럼의 결측치가 50%를 초과합니다"
- 행 수 < 10: WARNING "데이터가 부족합니다 (최소 10행 권장)"
- 행 수 > 100,000: INFO "대용량 데이터 — 자동 리샘플링 적용됩니다"

---

## 2. 금융 지표 계산 규칙

> 대응 모듈: `src/engine/financialEngine.ts`

### 전제 조건
- 무위험 수익률 (Rf): 연 3.5% (한국 기준금리 기준), 일간 환산: 0.035 / 252
- 연환산 기준: 252 거래일
- 최소 데이터 요건: 샤프/소르티노 = 최소 20일, CAGR = 최소 365일

### 수익률 지표

#### 총 수익률 (Total Return)
- 공식: (마지막_종가 - 첫번째_종가) / 첫번째_종가 × 100
- 단위: %
- 표시: +12.54% (양수에 반드시 + 기호)
- 색상 코딩: 양수=#26a69a / 음수=#ef5350 / 0=#90caf9

#### 일간 수익률 배열 (Daily Returns)
- 공식: (종가[i] - 종가[i-1]) / 종가[i-1]
- 용도: 샤프비율, 소르티노비율, 변동성 계산의 기반

#### CAGR (연환산 복리 수익률)
- 공식: (종료값 / 시작값)^(1 / 연수) - 1
- 연수 계산: (마지막날짜 - 첫번째날짜).days / 365.25
- 조건: 기간 < 1년 → null 반환 (표시: "N/A")

### 위험 조정 지표

#### 샤프 비율 (Sharpe Ratio)
공식:
```
daily_excess = 일간수익률_배열 - Rf_일간
sharpe = mean(daily_excess) / std(daily_excess) × √252
```
조건: 데이터 < 20일 → null

해석 등급:
- sharpe < 0: "위험 대비 수익 부족" (색상: #ef5350)
- 0 ≤ sharpe < 1: "보통" (색상: #ffa726)
- 1 ≤ sharpe < 2: "양호" (색상: #26a69a)
- sharpe ≥ 2: "우수" (색상: #4caf50)

#### 소르티노 비율 (Sortino Ratio)
공식:
```
downside_returns = 일간수익률_배열 중 Rf_일간 미만인 값
downside_std = std(downside_returns) × √252
sortino = (연환산_수익률 - 연_Rf) / downside_std
```
조건: 하방수익률이 0개일 경우 → null

#### 최대 낙폭 (MDD, Maximum Drawdown)
공식:
```
누적최고점 = cummax(종가_배열)
낙폭 = (누적최고점 - 현재종가) / 누적최고점
MDD = max(낙폭)
```
표시: -25.3% (항상 음수로 표현). 시각화: 낙폭 구간에 반투명 빨간 음영

#### 변동성 (Annualized Volatility)
- 공식: std(일간수익률_배열) × √252 × 100
- 단위: %

### 기술적 지표

#### 이동평균 (Simple Moving Average)
- 공식: MA_N[i] = mean(종가[i-N+1 .. i])
- 조건: i < N-1 → null
- 지원 기간: 20일, 50일, 200일

#### RSI (Relative Strength Index)
기간: 14일
공식:
```
변화량[i] = 종가[i] - 종가[i-1]
평균상승 = 14일간 상승분의 EMA (초기: 단순평균)
평균하락 = 14일간 하락분의 EMA (초기: 단순평균)
RS = 평균상승 / 평균하락
RSI = 100 - (100 / (1 + RS))
```
조건: i < 14 → null

해석:
- RSI > 70 → 과매수 (색상: #ef5350)
- RSI < 30 → 과매도 (색상: #26a69a)
- 30 ≤ RSI ≤ 70 → 중립

#### 볼린저 밴드 (Bollinger Bands)
기간: 20일, 표준편차 배수: 2
공식:
```
중심선 = MA20
표준편차 = std(종가[i-19..i])
상단 = MA20 + 2 × 표준편차
하단 = MA20 - 2 × 표준편차
```
조건: i < 19 → null

#### MACD
- 빠른 EMA: 12일, 느린 EMA: 26일, 시그널: 9일
- 공식: MACD = EMA12 - EMA26 / Signal = EMA9(MACD) / Histogram = MACD - Signal
- 조건: i < 25 → null

---

## 3. 시각화 선택 규칙

> 대응 모듈: `src/components/charts/`, `src/components/layout/DashboardShell.tsx`

### 핵심 원칙
1. 단일 차트 = 단일 인사이트 (하나의 차트가 하나의 메시지만 전달)
2. 금융 표준 색상 시스템 (전역 고정, 절대 변경 금지):
   - 상승/수익: #26a69a (teal-green)
   - 하락/손실: #ef5350 (red)
   - 중립/보조: #90caf9 (light-blue)
   - 경고: #ffa726 (orange)
3. 모든 차트는 인터랙티브 (툴팁, 호버 하이라이트) 지원
4. 다크 테마 전용: 배경 #0a0a0a, 카드 #111111

### 데이터 타입별 차트 선택 기준

#### STOCK_OHLCV → CandlestickChart
PRIMARY 차트:
- 라이브러리: lightweight-charts v5
- x축: 날짜 (YYYY-MM-DD)
- 바디: open/high/low/close (상승=#26a69a, 하락=#ef5350)
- 오버레이 1: MA20 (amber #f59e0b, lineWidth=1)
- 오버레이 2: MA50 (indigo #818cf8, lineWidth=1)
- 오버레이 3: 볼린저 밴드 (점선, rgba(96,165,250,0.7))
- 하단 서브패널: 거래량 히스토그램 (상승/하락 색상 매칭)

SECONDARY 차트:
- RSI(14) 라인 차트 — 70선(빨간)/50선(회색)/30선(teal) 기준선 표시
- 누적 수익률 AreaChart
- MDD 낙폭 차트 (빨간 그라데이션)

#### PORTFOLIO → TreemapChart + BarChart
PRIMARY 차트 (트리맵):
- 라이브러리: recharts Treemap
- 크기: weight/allocation 값
- 색상: 수익률 기준 (수익>#26a69a, 손실>#ef5350)
- 레이블: 종목명 + 비중%

SECONDARY 차트:
- 종목별 수익률 비교 BarChart (수평 방향)

#### RETURNS → ReturnsLineChart
PRIMARY 차트:
- 누적 수익률 곡선 (AreaChart)
- 낙폭 음영 (빨간색 반투명)
- 벤치마크가 있을 경우: 비교선 (회색 점선)

SECONDARY 차트:
- MDD 낙폭 차트

### 공통 시각화 규칙

#### 데이터 밀도 처리
- 데이터 포인트 > 1,000: 일간→주간 자동 리샘플링
- 데이터 포인트 > 5,000: 일간→월간 자동 리샘플링

#### 반응형 규칙
- 모바일 (<768px): 차트 1열, 높이 250px
- 태블릿 (768~1024px): 차트 2열, 높이 300px
- 데스크탑 (≥1024px): 차트 2~3열, 높이 360px 이상

#### 축 포맷
- 가격: 원화=₩#,### / 달러=$#.##
- 수익률: +12.5% (양수에 + 명시)
- 거래량: 1K/1M/1B 약어 또는 만/억 단위 사용
- 날짜: YYYY.MM.DD (한국 형식)

---

## 4. AI 인사이트 생성 규칙

> 대응 모듈: `src/engine/insightGenerator.ts`

### 시스템 역할
당신은 10년 경력의 전문 퀀트 애널리스트입니다. 제공된 금융 데이터와 계산된 지표를 기반으로 한국어로 명확하고 실용적인 투자 인사이트를 생성합니다. 모든 분석은 과거 데이터 기반이며 미래 수익을 보장하지 않습니다.

### 인사이트 생성 원칙
1. 가장 두드러진 수치 변화를 먼저 언급
2. 위험 요소를 기회 요소보다 먼저 언급 (보수적 원칙)
3. 숫자는 반드시 포함 (막연한 "좋다"보다 "샤프비율 1.8로 양호")
4. 최대 5개 인사이트, 각각 2~3문장으로 구성
5. 카테고리 접두사로 빠른 스캔 지원 (추세 / 모멘텀 / 리스크 / 거래량 / 변동성)

### 데이터 타입별 인사이트 템플릿

#### STOCK_OHLCV 인사이트
입력 지표: totalReturn, priceChange1d, volume, ma20, ma50, rsi14, volatility, mdd, sharpe, bollinger

출력 형식:
```
추세: "[추세 방향] — MA20([ma20값])과 MA50([ma50값])의 [관계]. [괴리율 X.XX%로 신호 해석]"
모멘텀: "RSI([rsi14값]) — [과매수/과매도/중립] 구간. [단기 시사점 2문장]"
리스크: "변동성 [volatility]%, 최대 낙폭 [mdd]%. 샤프비율 [sharpe]([등급])로 [평가]"
```

예시 출력:
```
추세: "단기 상승 추세 — MA20(72,400원)이 MA50(69,800원) 위에 위치하여 골든크로스 구도를 형성하고 있습니다. 괴리율 3.72%로 단기 모멘텀이 유효한 상태입니다."
모멘텀: "RSI(67.2) — 과매수 임박 구간입니다. 추가 상승 여력은 있으나 단기 조정 가능성에 주의하세요."
리스크: "연환산 변동성 22.3%, 최대 낙폭 -18.7%입니다. 샤프비율 1.4로 양호한 위험 대비 수익을 보이고 있습니다."
```

#### PORTFOLIO 인사이트
입력 지표: totalReturn, sharpeRatio, sortinoRatio, mdd, topAsset, worstAsset, assetCount, topConcentration

출력 형식:
```
성과: "포트폴리오 수익률 [totalReturn]%, 샤프비율 [sharpeRatio]([등급]). [수익 맥락 해석]"
위험: "최대 낙폭 [mdd]%, [topAsset]에 [topConcentration]% 집중. [분산 평가 2문장]"
개선: "[worstAsset] 비중 점검 및 [구체적 대안] 고려 권고 (과거 데이터 기반)"
```

#### RETURNS 인사이트
입력 지표: totalReturn, cagr, sharpeRatio, sortinoRatio, mdd, volatility

출력 형식:
```
수익성: "총 수익률 [totalReturn]%, CAGR [cagr]%로 [벤치마크 비교 또는 절대 평가]. [맥락 설명]"
효율성: "샤프비율 [sharpeRatio]([등급]) — 단위 리스크당 수익이 [평가]합니다. 소르티노 [sortinoRatio]로 하방 리스크도 [평가]."
하방: "최대 낙폭 [mdd]%, 변동성 [volatility]%. [전체 리스크 수준 종합 평가]"
```

### 수치 해석 기준

샤프비율:
- ≥ 2.0: "매우 우수"
- 1.0~2.0: "양호"
- 0~1.0: "보통"
- < 0: "위험 대비 수익 부족"

변동성(연환산):
- < 10%: "저변동"
- 10~20%: "중간 변동"
- 20~30%: "고변동"
- > 30%: "매우 고변동"

MDD:
- > -10%: "낮은 낙폭"
- -10~-20%: "보통 낙폭"
- -20~-40%: "높은 낙폭"
- < -40%: "매우 높은 낙폭 — 리스크 재검토 필요"

RSI:
- > 80: "심한 과매수 — 단기 급락 주의"
- 70~80: "과매수 — 조정 가능성 유의"
- 55~70: "중립 상단 — 상승 모멘텀 유지"
- 45~55: "중립 — 방향성 탐색 구간"
- 30~45: "중립 하단 — 하락 압력 존재"
- < 30: "과매도 — 단기 반등 가능성"
- < 20: "심한 과매도 — 기술적 반등 구간"

### 절대 금지 사항
- "매수하세요", "투자하세요" 등 직접적 투자 권유
- "반드시 오릅니다", "확실합니다" 등 미래 확정 표현
- 근거 없는 추측이나 예측
- 모든 분석은 "과거 데이터 기반" 면책 문구 포함

### 응답 JSON 형식
```json
{
  "insights": [
    "추세: ...",
    "모멘텀: ...",
    "리스크: ..."
  ],
  "disclaimer": "본 분석은 과거 데이터 기반이며 미래 수익을 보장하지 않습니다."
}
```

### 규칙 기반 폴백 (API 키 없을 때)
Claude API 키가 없는 경우 자동으로 규칙 기반 인사이트가 생성됩니다.
- MA 골든크로스 / 데드크로스 감지 + 괴리율 계산
- RSI 7단계 구간 해석
- 볼린저 밴드 스퀴즈 감지 (밴드폭 4%/6% 임계값)
- 거래량 이상 탐지 (20일 평균 대비 +100%, -70% 기준)
- MDD + 변동성 등급 자동 분류

---

## 5. 대시보드 레이아웃 규칙

> 대응 모듈: `src/components/layout/DashboardShell.tsx`, `src/components/layout/KPIBar.tsx`

### 전체 레이아웃 원칙
1. F-Pattern: 최상단 KPI 요약 → 주요 차트 → 보조 분석 → 상세 테이블
2. 다크 테마 전용 (금융 대시보드 표준)
3. 모바일 우선 반응형 (Mobile-First Responsive)
4. 컴포넌트 최소 단위: 카드 (bg-[#111111], border border-[#222222])

### 색상 토큰 시스템 (절대 변경 금지)
```
배경:          #0a0a0a  (최외곽)
카드 배경:      #111111  (컴포넌트)
보더:          #222222
텍스트 기본:    #d4d4d4
텍스트 보조:    #888888
텍스트 약함:    #555555
상승/수익:      #26a69a
하락/손실:      #ef5350
MA20:          #f59e0b  (amber)
MA50:          #818cf8  (indigo)
볼린저 밴드:    rgba(96,165,250,0.7)
```

### KPI Bar 스펙 (최상단 고정)
- 위치: 헤더 바로 아래, 전체 너비 (스크롤 가능)
- 카드 내부 구성:
  - 레이블: text-xs text-[#888888] uppercase tracking-wider
  - 수치: text-xl font-bold, 색상 자동 코딩 (양수=teal / 음수=red)
  - 단위: text-sm text-[#555555]

### STOCK_OHLCV 레이아웃
```
┌──────────────────────────────────────────────────────┐
│ KPI Bar: 현재가 | 수익률 | CAGR | 샤프 | 소르티노     │
│          MDD | 변동성 | RSI                           │
├─────────────────────────┬────────────────────────────┤
│ 캔들스틱 차트 (col-3)    │ AI 인사이트 패널 (col-2)  │
│ + MA20/MA50 오버레이     │                            │
│ + 볼린저 밴드            │                            │
│ [거래량 서브패널]        │                            │
├─────────────────────────┴────────────────────────────┤
│ RSI 패널 (col-2) | 누적 수익률 차트 (col-2)           │
│                  | MDD 낙폭 차트                      │
├──────────────────────────────────────────────────────┤
│ 원시 데이터 테이블 (접기/펼치기)                       │
└──────────────────────────────────────────────────────┘
```

### PORTFOLIO 레이아웃
```
┌──────────────────────────────────────────────────────┐
│ KPI Bar: 총수익률 | 샤프비율 | MDD | 변동성 | 종목수  │
├─────────────────────────┬────────────────────────────┤
│ 트리맵 (col-3)           │ AI 인사이트 패널 (col-2)  │
├─────────────────────────┴────────────────────────────┤
│ 종목별 수익률 바 차트                                  │
├──────────────────────────────────────────────────────┤
│ 포트폴리오 보유 현황 테이블                            │
└──────────────────────────────────────────────────────┘
```

### RETURNS 레이아웃
```
┌──────────────────────────────────────────────────────┐
│ KPI Bar: 총수익률 | CAGR | 샤프비율 | MDD | 변동성   │
├─────────────────────────┬────────────────────────────┤
│ 누적 수익률 차트 (col-3) │ AI 인사이트 패널 (col-2)  │
├─────────────────────────┴────────────────────────────┤
│ MDD 낙폭 차트                                         │
└──────────────────────────────────────────────────────┘
```

### 인터랙션 규칙
- 차트 호버: 툴팁으로 정확한 수치 표시
- 차트 확대: 핀치/드래그 줌 지원 (lightweight-charts 기본 제공)
- 테이블 정렬: 컬럼 클릭 시 오름/내림차순
- 반응형 전환: CSS Grid 자동 조정 (grid-cols-1 → grid-cols-4)
- 다크 테마 고정 (라이트 모드 전환 없음)

### 인사이트 패널 렌더링
각 인사이트는 3단 계층으로 렌더링:
1. 접두사(prefix): text-[10px] text-[#555555] uppercase tracking-widest
2. 핵심 문장(headline): text-sm text-[#d4d4d4] leading-snug
3. 상세 설명(detail): text-xs text-[#606060] leading-relaxed mt-1

로딩 상태: 스켈레톤 애니메이션 (pulse 효과, 3라인 구조)
